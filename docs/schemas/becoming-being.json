{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://be-framework.org/schemas/becoming-being.json",
  "title": "Becoming Being Context",
  "description": "Close context for a metamorphosis that produced a new being which will itself continue transforming.",
  "type": "object",
  "required": ["being", "prop"],
  "properties": {
    "being": {
      "type": "string",
      "description": "Fully-qualified class name of the new being."
    },
    "prop": {
      "type": "object",
      "description": "Public readonly properties of the new being (excluding the Been log carrier).",
      "additionalProperties": true
    }
  },
  "additionalProperties": false
}
